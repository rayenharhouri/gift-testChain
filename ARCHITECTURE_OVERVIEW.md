# GIFT Blockchain - Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        GIFT BLOCKCHAIN SYSTEM                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    LAYER 5: DOCUMENTS & DISPUTE                     │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  DocumentRegistry                                           │   │   │
│  │  │  • Document hash storage      • Merkle root anchoring       │   │   │
│  │  │  • Integrity verification     • Set of Documents (SOD)      │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │ references                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                  LAYER 4: TRANSACTION & LOGISTICS                   │   │
│  │  ┌──────────────────────────┐  ┌──────────────────────────────┐    │   │
│  │  │  TransactionOrderBook    │  │  TransactionEventLogger      │    │   │
│  │  │  • Order state machine   │  │  • Unified event log         │    │   │
│  │  │  • Multi-sig validation  │──▶│  • Audit trail              │    │   │
│  │  │  • Settlement execution  │  │  • Off-chain indexing       │    │   │
│  │  └──────────────────────────┘  └──────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │ calls                                  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    LAYER 3: ACCOUNT & LEDGER                        │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  GoldAccountLedger                                          │   │   │
│  │  │  • IGAN management       • Balance tracking                 │   │   │
│  │  │  • Asset transfers       • Lock/unlock (pledge, transit)    │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │ updates                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      LAYER 2: ASSET & VAULT                         │   │
│  │  ┌──────────────────────────┐  ┌──────────────────────────────┐    │   │
│  │  │  GoldAssetToken (ERC1155)│  │  VaultRegistry              │    │   │
│  │  │  ✅ IMPLEMENTED          │  │  ⏳ PENDING                 │    │   │
│  │  │  • ERC1155 NFT           │  │  • Vault site registry      │    │   │
│  │  │  • Immutable attributes  │  │  • Individual vaults        │    │   │
│  │  │  • Status management     │  │  • Capacity tracking        │    │   │
│  │  └──────────────────────────┘  └──────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    ▲                                        │
│                                    │ validates                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                       LAYER 1: IDENTITY                             │   │
│  │  ┌─────────────────────────────────────────────────────────────┐   │   │
│  │  │  MemberRegistry                                             │   │   │
│  │  │  ✅ IMPLEMENTED                                             │   │   │
│  │  │  • Member GIC management                                    │   │   │
│  │  │  • User registration                                        │   │   │
│  │  │  • Role assignment (8 roles)                                │   │   │
│  │  │  • Permission validation                                    │   │   │
│  │  └─────────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow: Asset Registration to Transfer

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         ASSET LIFECYCLE FLOW                                │
└─────────────────────────────────────────────────────────────────────────────┘

1. REGISTRATION
   ┌──────────────────────────────────────────────────────────────┐
   │ Refiner calls: GoldAssetToken.mint()                         │
   │ ├─ Check: isMemberInRole(refiner, ROLE_REFINER)             │
   │ │  └─ MemberRegistry validates refiner has REFINER role     │
   │ ├─ Prevent: Duplicate (serial + refiner)                    │
   │ ├─ Create: ERC1155 NFT with immutable attributes            │
   │ └─ Emit: AssetMinted event                                  │
   └──────────────────────────────────────────────────────────────┘
                              ↓
2. VAULT STORAGE
   ┌──────────────────────────────────────────────────────────────┐
   │ Custodian calls: GoldAssetToken.updateStatus()              │
   │ ├─ Check: isMemberInRole(custodian, ROLE_CUSTODIAN)         │
   │ ├─ Update: Status = IN_VAULT                                │
   │ └─ Emit: AssetStatusChanged event                           │
   └──────────────────────────────────────────────────────────────┘
                              ↓
3. TRANSFER (Future - GoldAccountLedger)
   ┌──────────────────────────────────────────────────────────────┐
   │ Trader calls: GoldAccountLedger.transferAsset()             │
   │ ├─ Check: isMemberInRole(trader, ROLE_TRADER)              │
   │ ├─ Validate: Asset not locked                               │
   │ ├─ Update: Ownership in GoldAssetToken                      │
   │ ├─ Update: Account balances                                 │
   │ └─ Emit: AssetTransferred event                             │
   └──────────────────────────────────────────────────────────────┘
                              ↓
4. BURN/DELIVERY
   ┌──────────────────────────────────────────────────────────────┐
   │ Owner calls: GoldAssetToken.burn()                          │
   │ ├─ Check: Owner or PLATFORM role                            │
   │ ├─ Update: Status = BURNED                                  │
   │ ├─ Burn: ERC1155 token                                      │
   │ └─ Emit: AssetBurned event                                  │
   └──────────────────────────────────────────────────────────────┘
```

---

## Authorization Model

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    THREE-LEVEL AUTHORIZATION VALIDATION                     │
└─────────────────────────────────────────────────────────────────────────────┘

Level 1: Address Linked to Member
┌─────────────────────────────────────────────────────────────┐
│ addressToMemberGIC[0x123...] = "GIFTCHZZ"                  │
│ ✓ Address is linked to a member                            │
│ ✗ Address has no member association                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
Level 2: Member is ACTIVE
┌─────────────────────────────────────────────────────────────┐
│ members["GIFTCHZZ"].status = ACTIVE                        │
│ ✓ Member is active and can perform operations              │
│ ✗ Member is SUSPENDED or TERMINATED                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
Level 3: Member has Required Role
┌─────────────────────────────────────────────────────────────┐
│ members["GIFTCHZZ"].roles & ROLE_REFINER != 0              │
│ ✓ Member has the required role                             │
│ ✗ Member lacks the required role                           │
└─────────────────────────────────────────────────────────────┘
                              ↓
                    ✅ AUTHORIZED
```

---

## Role Hierarchy

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           8 ROLE TYPES                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ROLE_REFINER (1 << 0)                                                     │
│  └─ Can mint new gold assets                                               │
│  └─ Can register assets on blockchain                                      │
│                                                                             │
│  ROLE_TRADER (1 << 1)                                                      │
│  └─ Can create and execute transactions                                    │
│  └─ Can transfer assets between accounts                                   │
│                                                                             │
│  ROLE_CUSTODIAN (1 << 2)                                                   │
│  └─ Can manage custody arrangements                                        │
│  └─ Can update asset status                                                │
│                                                                             │
│  ROLE_VAULT_OP (1 << 3)                                                    │
│  └─ Can manage vault sites and vaults                                      │
│  └─ Can track capacity and utilization                                     │
│                                                                             │
│  ROLE_LSP (1 << 4)                                                         │
│  └─ Logistics Service Provider                                             │
│  └─ Can track asset movements                                              │
│                                                                             │
│  ROLE_AUDITOR (1 << 5)                                                     │
│  └─ Can perform audits                                                     │
│  └─ Can verify asset integrity                                             │
│                                                                             │
│  ROLE_PLATFORM (1 << 6)                                                    │
│  └─ Platform administrator                                                 │
│  └─ Can register members and users                                         │
│  └─ Can suspend members                                                    │
│                                                                             │
│  ROLE_GOVERNANCE (1 << 7)                                                  │
│  └─ Governance council member                                              │
│  └─ Can approve members                                                    │
│  └─ Can assign/revoke roles                                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Member Lifecycle

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         MEMBER STATE MACHINE                                │
└─────────────────────────────────────────────────────────────────────────────┘

                    registerMember()
                          │
                          ▼
                    ┌──────────────┐
                    │   PENDING    │  ← Initial state
                    └──────────────┘
                          │
                          │ approveMember() [GOVERNANCE]
                          ▼
                    ┌──────────────┐
                    │   ACTIVE     │  ← Can perform operations
                    └──────────────┘
                      ▲         │
                      │         │ suspendMember() [PLATFORM]
                      │         ▼
                      │    ┌──────────────┐
                      │    │  SUSPENDED   │  ← Cannot perform operations
                      │    └──────────────┘
                      │         │
                      │         │ (cannot reactivate)
                      │         ▼
                      │    ┌──────────────┐
                      │    │ TERMINATED   │  ← Final state
                      │    └──────────────┘
                      │
                      └─ (can be reactivated from SUSPENDED)
```

---

## User Lifecycle

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          USER STATE MACHINE                                 │
└─────────────────────────────────────────────────────────────────────────────┘

                    registerUser()
                          │
                          ▼
                    ┌──────────────┐
                    │   ACTIVE     │  ← Initial state
                    └──────────────┘
                          │
                          │ suspendUser() [PLATFORM]
                          ▼
                    ┌──────────────┐
                    │  SUSPENDED   │  ← Cannot access
                    └──────────────┘
                          │
                          │ activateUser() [PLATFORM]
                          ▼
                    ┌──────────────┐
                    │   ACTIVE     │  ← Reactivated
                    └──────────────┘
```

---

## Data Structures

### Member
```solidity
struct Member {
    string memberGIC;           // Global ID (e.g., "GIFTCHZZ")
    string entityName;          // Organization name
    string country;             // ISO country code
    MemberType memberType;      // INDIVIDUAL, COMPANY, INSTITUTION
    MemberStatus status;        // PENDING, ACTIVE, SUSPENDED, TERMINATED
    uint256 createdAt;          // Block timestamp
    uint256 updatedAt;          // Last update timestamp
    bytes32 memberHash;         // Hash of off-chain data (no PII)
    uint256 roles;              // Bitwise role flags
}
```

### User
```solidity
struct User {
    string userId;              // Unique user ID
    bytes32 userHash;           // Hash of identity (no PII)
    string linkedMemberGIC;     // Associated member
    UserStatus status;          // ACTIVE, INACTIVE, SUSPENDED
    uint256 createdAt;          // Block timestamp
    address[] adminAddresses;   // Authorized wallet addresses
}
```

### GoldAsset
```solidity
struct GoldAsset {
    string tokenId;             // GIFT-ASSET-YYYY-NNNNN
    string serialNumber;        // Refiner serial
    string refinerName;         // Manufacturer name
    uint256 weightGrams;        // Gross weight (scaled by 10^4)
    uint256 fineness;           // Purity (9999 = 99.99%)
    uint256 fineWeightGrams;    // Calculated: weight × fineness / 10000
    GoldProductType productType; // BAR, COIN, DUST, OTHER
    bytes32 certificateHash;    // Certificate authenticity
    string traceabilityGIC;     // Introducing member
    AssetStatus status;         // REGISTERED, IN_VAULT, PLEDGED, BURNED
    uint256 mintedAt;           // Block timestamp
    bool certified;             // LBMA certification
}
```

---

## Implementation Status

| Component | Status | Tests | Functions | Lines |
|-----------|--------|-------|-----------|-------|
| MemberRegistry | ✅ Complete | 19 | 18 | 280 |
| GoldAssetToken | ✅ Complete | 9 | 10 | 320 |
| VaultRegistry | ⏳ Pending | - | - | - |
| GoldAccountLedger | ⏳ Pending | - | - | - |
| TransactionOrderBook | ⏳ Pending | - | - | - |
| TransactionEventLogger | ⏳ Pending | - | - | - |
| DocumentRegistry | ⏳ Pending | - | - | - |

---

## Key Achievements

✅ **Robust Authorization System**
- Three-level validation (address → member → role)
- Bitwise role flags for efficiency
- Bootstrap mechanism for initialization

✅ **Secure Asset Tokenization**
- ERC1155 standard compliance
- Immutable asset attributes
- Duplicate prevention
- Certificate verification

✅ **Comprehensive Testing**
- 28/28 tests passing
- 100% specification alignment
- Production-grade code quality

✅ **Complete Documentation**
- Inline code comments
- External specifications
- Architecture diagrams
- Test coverage reports

---

## Next Phase: VaultRegistry

**Purpose:** Manage physical gold storage facilities

**Key Features:**
- Register vault sites with location and insurance info
- Create individual vaults within sites
- Track capacity and utilization
- Record audit dates and compliance

**Estimated Timeline:** 4-5 days

---

**Status:** 🟢 Phase 1 Complete - Ready for Phase 2
